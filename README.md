# God Damn Repackager

> **⚠️ ALPHA 版本（0.2.0）**
> 本模组目前处于早期测试阶段。它通过 Mixin 修改机械动力（Create）的理包机核心逻辑，
> **尚未经过大规模、长时间的稳定性测试**。已知风险包括（但不限于）：
> - 在未预见的边界场景下可能导致**物品丢失或异常**
> - 与特定版本的机械动力或其它模组可能存在兼容性问题
> - 游戏崩溃、世界存档异常
>
> **使用前请务必备份你的存档。** 作者不对因使用本模组造成的任何数据损失负责。
> 如果遇到 bug，欢迎在 GitHub Issues 提交（附上 `crash-reports/` 下的崩溃报告和游戏日志）。

A Forge mod for Minecraft 1.20.1 that makes Create 6.0+'s Repackagers parallelize a single large
crafting order across all repackagers sharing the same input vault.

---

## 这个模组解决什么问题

在原版机械动力 6.0+ 中，当你通过仓库管理员（Stockkeeper）对一个合成器阵列下达一个大批量合成订单时
（例如"合成 1000 个铁块"），**整张订单只会被一个理包机（Repackager）处理**。即使你的输入保险库
周围贴满了理包机，也只有其中一个在忙活，其余的全部闲置。一个理包机每秒只能输出一个包裹（动画周期
20 ticks），所以大订单要等很久。

本模组让**同一保险库上的所有理包机并行分担**这张订单。N 个理包机 ≈ N 倍的处理速度。

### 效果对比

| 场景 | 原版 | 加上本模组 |
|---|---|---|
| 1000 个合成，1 个理包机 | ~1000 秒 | ~1000 秒（无变化） |
| 1000 个合成，3 个理包机 | ~1000 秒（其余 2 个闲置） | ~333 秒 |
| 1000 个合成，9 个理包机 | ~1000 秒（其余 8 个闲置） | ~111 秒 |

## 工作原理（简述）

理包机在整理订单时会一次性把整张订单的合成包裹塞进**自己的**发送队列，然后每秒发出一个。
本模组拦截这个"塞进队列"的动作，改为把包裹按**负载均衡**分配给同一保险库上的所有理包机——
队列更短的理包机分到更多活，这样所有机器的产能都被利用。

详细的技术实现见 [TECHNICAL.md](TECHNICAL.md)。

## 安装

1. 安装 **Minecraft 1.20.1 + Forge 47.x**
2. 安装 **Create 6.0.x**（已测试 6.0.8）
3. 把 `goddamnrepackager-0.2.0.jar` 放进 `.minecraft/mods/` 文件夹
4. 启动游戏

不需要任何配置，开箱即用。

## 使用方法

**不需要任何额外操作。** 像平时一样搭你的合成器阵列：

```
[总仓库] ──订单──> 蛙港发货 ──> 输入保险库(装着原料包裹)
                                       ↓
                            多个理包机(贴红石块常开)
                                       ↓
                            打包机(拆包) ──> 动力合成器 ──> 产物
```

只要输入保险库周围有多个理包机，本模组就会自动让它们并行工作。

## 兼容性

- ✅ 已测试：Minecraft 1.20.1 + Forge 47.2.0 + Create 6.0.8
- ✅ 已在含其它模组的环境和多人服务器中测试，无冲突
- ⚠️ 仅针对 Create 6.0.x 的物流系统设计；不兼容 Create 0.5.1 及更早版本
- ⚠️ 仅 Forge 版本（Fabric 移植见 [TECHNICAL.md](TECHNICAL.md) 的后续方向）

## 已知限制

- 当前实现（思路 A：负载均衡分配）是"一次性快照分配"——在理包机整理出订单包裹的那一瞬间决定
  谁分到多少。如果在那之后有理包机空闲下来，它不会去"抢"别的理包机队列里的活。
  在绝大多数实际场景下这已经足够；极端情况下的动态负载均衡（思路 B）见技术文档。
- 理包机必须贴在**机械动力的保险库（Vault）**上。其它容器（板条箱、原版箱子）理论上也支持，
  但尚未充分测试。

## 调试

如果需要排查问题，可以在 `GodDamnRepackager.java` 里把 `DEBUG_LOGGING = true` 重新编译。
开启后每次订单分配都会在日志里打印 `[GDR-DIST]` 行，显示每个理包机分到了多少包裹。

## 许可证

MIT License — 自由使用、修改、分发。

## 致谢

- [Create](https://github.com/Creators-of-Create/Create) 及其作者 simibubi —— 优秀的机械动力模组
- 本模组的项目名"God Damn Repackager"表达了作者在游玩时遇到原始限制时的心情
